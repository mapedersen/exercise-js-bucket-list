// create a new paragraph
const newParagraph = document.createElement("div");

// add content to the new paragraph
newParagraph.innerText = "Sell a SaaS";

// append new paragraph to dom
document.body.appendChild(newParagraph);